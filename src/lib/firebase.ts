// This file is intentionally left blank to disable the Firebase client SDK.
// This is a temporary measure to resolve persistent build errors.
// A proper authentication solution will be implemented in the future.

// We export null values to avoid breaking imports in other files.
export const app = null;
export const auth = null;
